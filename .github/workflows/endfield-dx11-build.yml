name: endfield-dx11-build

# Trimmed copy of nightly.yml: builds ONLY the Endfield addon (x64) from whichever branch it is
# dispatched on, and uploads it as a workflow artifact instead of publishing a release.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up shader tools
        shell: pwsh
        run: .\scripts\setup-dev-env.ps1 -Update -Tools dxc,slang,glslang

      - name: Set up Clang
        uses: egor-tensin/setup-clang@v1
        with:
          version: latest
          platform: x64

      - name: Install ninja-build tool
        uses: seanmiddleditch/gha-setup-ninja@v5

      - name: Configure build for amd64
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: amd64

      - name: Configure CMake (x64)
        run: cmake --preset clang-x64

      - name: Build endfield addon (x64)
        run: cmake --build --preset clang-x64-release --target endfield --verbose

      - name: Show build output
        shell: pwsh
        run: |
          git rev-parse HEAD
          Get-ChildItem build\release\renodx-endfield.*
          Get-FileHash -Algorithm MD5 build\release\renodx-endfield.addon64

      - name: Upload addon
        uses: actions/upload-artifact@v4
        with:
          name: renodx-endfield-dx11-${{ github.sha }}
          path: |
            build/release/renodx-endfield.addon64
            build/release/renodx-endfield.pdb
          if-no-files-found: error
